Converts cached wal colors into an importable iTerm colorscheme. These themes are stored in the wal cache directory under `itermcolors`

```bash
joe ~ wal -i "IMAGE" -o "./contrib/wal2iterm/wal2iterm"
```

